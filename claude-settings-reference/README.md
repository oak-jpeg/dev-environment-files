Reference copy of `~/.claude/settings.json` (NOT symlinked on purpose — same
reasoning as mehd-io/dotfiles: this file loads at Claude Code startup, so it's
kept as a real, reviewed file rather than a stow symlink to avoid the public
checkout silently changing live startup behavior). Update this reference copy
by hand after editing the live file, if you want the repo to reflect it.
